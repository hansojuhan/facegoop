<%# This will generate a string like this: likes_post_1 %>
<%# This gives us a target to be used by turbo stream %>
<%= tag.div id: dom_id(post, :like_button) do %>
  <%= button_to like_button_text(post), post_like_path(post), method: :patch, class: "mt-2 rounded-lg py-3 px-5 inline-block font-medium bg-gray-100" %>
<% end %>
