<div class="mx-auto md:w-2/3 w-full">
  <%= link_to "← Back to the post", post_path(@post), class: "link-tertiary mb-4" %>
  
  <h4 class="font-bold text-4xl">Edit the post</h4>
  <%= render "form", post: @post %>
</div>
